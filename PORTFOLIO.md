# Dialogue Studio — AI 对话故事创作平台

> **一句话定位**：给一个主题，得到一段 AI 生成的对话故事、一组精美的手机聊天卡片、以及一段带配音的竖屏短视频。
> 
> **求职方向**：AI 产品经理 | 用 Vibe Coding 从 0 到 1 驱动产品落地

---

## 1. 产品背景：我发现了什么痛点

短视频平台上，"AI 与人类对话"体裁的内容（治愈向、陪伴感、情感留白）流量很好，但创作者面临三重痛苦：

1. **写故事难** — 5 张卡片、30 句台词，每句要符合角色性格、节奏递进、不说教
2. **做卡片烦** — 每张都要排版、选配色、对齐气泡，工具要么太重（Figma）要么太丑（PPT）
3. **出视频更烦** — 配音、字幕、时间轴、转场、波形可视化，传统视频工具门槛极高

**我的判断**：这不是一个"更好用的编辑器"的问题，而是一个"从主题到成片的端到端自动化"的问题。

---

## 2. 产品形态：三件产物

| 产物 | 形态 | 用户价值 |
|---|---|---|
| **故事文本** | JSON（结构化对话脚本） | 可二次编辑、可导入其他工具 |
| **HTML 卡片** | 静态网页，模拟手机聊天界面 | 可直接截图发小红书/朋友圈，也可浏览器打开预览 |
| **短视频** | 1080×1920 竖屏 MP4 | 逐句弹出的对话动画 + 同步配音，直接发抖音/视频号 |

**核心约束**：每个产物都必须**独立可用**。不能为了做视频而绑架用户必须先接受自动生成的故事。

---

## 3. 我的产品决策过程

### 3.1 先做编辑器还是先做生成？

**决策**：先做工程基础（项目/台词 CRUD）和 AI 生成，再做编辑器。

**理由**：如果 AI 生成的故事质量不够，编辑器做得再好也没用。先做"端到端走通"验证故事质量，再补编辑器让用户打磨。这个顺序避免了"前端重投入后发现后端核心链路不通"的风险。

### 3.2 为什么从 Claude 切到 Kimi K2.6？

最初用 Claude API 生成故事，效果稳定。但在推进过程中发现：

- **Claude 在国内访问成本高**：需要代理，团队协作者接入门槛高
- **Kimi K2.6 完全兼容 OpenAI SDK**：接入成本 = 改一行 `base_url`，零学习成本
- **中文叙事质量相当**：测试同主题对比，Kimi 在"口语化短句"和"情感留白"上甚至略优
- **生态策略**：Moonshot 平台同时提供 TTS 和图像能力，未来扩展空间大

**决策**：切到 Kimi。整个迁移只改了 `fast/llm.py` 一个文件（约 60 行），1 小时完成验证。

### 3.3 同步 TTS vs 异步 TTS

MiniMax 提供两种 TTS 模式：
- **同步** `t2a_v2`：立刻返回音频 + `duration_ms`
- **异步** `t2a_async_v2`：提交后 poll 状态，可返回**句级时间戳**

我的第一反应是"异步更好，有时间戳就能做逐字高亮字幕"。但深入分析后：

- **同步 already 够用**：`duration_ms` 足够驱动"气泡按音频时长依次弹入"的动画
- **异步增加复杂度**：需要任务队列、poll 逻辑、失败重试、状态管理
- **产品验证优先**：当前形态（对话弹出）不需要逐字高亮，同步 TTS 更稳更快

**决策**：Phase 2 只用同步 TTS。异步链路留给"波形可视化 + 逐字字幕"的 v2 形态再考虑。

### 3.4 为什么从自写 ffmpeg 转向 HyperFrames？

Phase 3 原计划：异步 TTS 拿时间戳 → Python 调 ffmpeg 合并音轨 → PIL/MoviePy 渲染卡片 → 波形可视化。

问题：
- **链路太长**：4 个环节，每个都可能失败，调试成本指数级增长
- **视觉效果天花板低**：PIL 画圆角气泡、做动效、做转场，代码量巨大且效果有限
- **团队不可维护**：换一个人接手这套 ffmpeg pipeline 需要专门学习

转折点：发现 HyperFrames — 一个"HTML 即视频源文件"的渲染框架：
- **我们的卡片本来就是 HTML**：`.story-card` 的 CSS 可以直接复用
- **时间轴用 `data-*` 属性声明**：不需要写复杂的时间计算代码
- **GSAP 做动画**：行业标准，动效质量远超自写
- **渲染命令 = `npx hyperframes render`**：一个命令出片

**决策**：放弃异步 TTS + ffmpeg 路线，改用 HyperFrames。整个视频渲染逻辑从"几百行 Python + 外部依赖"压缩为"一个 Jinja 模板生成 HTML + 一行 CLI"。

---

## 4. 技术架构

```
输入：主题字符串
  ↓
[LLM] Kimi K2.6 → story.json（结构化对话脚本）
  ↓
[卡片渲染] story.json → cards.html（手机聊天界面）
  ↓
[TTS] MiniMax 同步 API → audio/{NN}.mp3 + lines.json（含 duration_ms）
  ↓
[视频合成] HyperFrames → composition/index.html → final.mp4
```

**技术选型理由**：
- **FastAPI + SQLite**：足够支撑单用户/小团队场景，零运维成本
- **单文件前端 `index.html`**：无构建步骤，改完即生效，适合快速迭代
- **Kimi K2.6**：OpenAI SDK 兼容，团队零迁移成本
- **MiniMax TTS**：国内 API，延迟低，支持 emotion/语气词标签
- **HyperFrames**：把"视频渲染"从工程问题变成"前端设计"问题，极大降低维护成本

---

## 5. Vibe Coding 实践：我是怎么和 AI 协作的

这个项目不是我一行行写出来的，而是**我和 Claude Code（AI 编程助手）协作完成的**。以下是我的协作方法论：

### 5.1 先定义边界，再写代码

任何实现之前，先和 AI 对齐"产品形态 + 技术约束"：
- 不允许直接调用 backend 代码（隔离原则）
- 文件系统即状态，每步可重跑
- 先 CLI 验证，再考虑 UI 化

这些约束写在 `agents.md` 里，成为后续所有代码修改的"宪法"。

### 5.2 用最小可行分支验证假设

当发现 HyperFrames 可能替代 ffmpeg 时，我没有在原有 backend 上直接改，而是开了 `fast/` 分支：
- 不依赖数据库、不依赖 FastAPI
- 纯命令行 + 文件系统
- 2 小时搭完骨架，1 小时跑通 dry-run（用占位音频验证 composition 结构）
- `npx hyperframes lint` 0 errors / 0 warnings 后，再决定反哺 backend

**这就是产品经理做实验的思路**：低成本验证假设，验证通过再规模化。

### 5.3 把技术决策文档化

每一个"为什么选 A 不选 B"都记录在案：
- `PROJECT.md`：完整技术上下文（给外部协作者）
- `agents.md`：开发规范（给在本仓库写代码的 AI/人）
- `fast/README.md`：CLI 用法（给使用者）

这让项目具备**可交接性**——不是"只有我懂"的黑箱。

---

## 6. 当前进展

| Phase | 内容 | 状态 |
|---|---|---|
| Phase 0 | 工程基础（FastAPI + SQLite + CRUD） | ✅ 完成 |
| Phase 1 | Kimi 故事生成 + 卡片编辑器 | ✅ 完成 |
| Phase 2 | MiniMax 同步 TTS（单句/批量 SSE/试听） | ✅ 完成 |
| Phase 3 | HyperFrames 视频渲染 | 🚧 验证通过，待端到端实测 |

**已验证**：
- `fast/` 分支的 dry-run 通过 `npx hyperframes lint`（0 errors / 0 warnings）
- composition 结构合规，气泡入场动画逻辑正确
- 音频时长驱动时间轴的拼接逻辑正确

**待完成**：
- 填真实 MiniMax voice_id，跑完整 audio → video 链路
- 评估渲染速度（draft/standard/high 三档质量对比）

---

## 7. 产品亮点（适合面试讲）

### 亮点 1："文件即状态"的极简设计哲学

`fast/` 分支没有任何数据库。每个故事的产物就是 `outputs/{slug}/` 目录下的文件：
- 删 `story.json` → 重新生成故事
- 删 `audio/03.mp3` → 只重新合成那一句
- 删 `composition/` → 重新生成视频

**产品意义**：状态透明、可回滚、零运维。用户（甚至非技术用户）可以直观地理解"删了这个文件就会重新做这个步骤"。

### 亮点 2：SSE 实时进度反馈

批量配音时，前端用 Server-Sent Events 实时接收每条台词的合成状态：
- `7/12 · 助理 生成中...`
- 单句失败不中断整批，标红显示错误信息，继续处理后面的

**产品意义**：长任务必须有进度感知，否则用户会以为卡死了。失败隔离比"全有或全无"更友好。

### 亮点 3：从"工程问题"到"设计问题"的范式转换

传统视频渲染是工程密集型（ffmpeg、时间轴计算、编解码）。引入 HyperFrames 后，视频渲染变成前端设计问题：
- 设计师改 CSS 就能改视频风格
- 产品经理调 `data-duration` 就能改节奏
- 不需要懂视频编码

**产品意义**：降低了视频生产的门槛，让"非视频专业"的创作者也能产出高质量内容。

---

## 8. 下一步规划

1. **跑通端到端链路**：用真实 voice_id 合成音频，渲染出第一条完整视频
2. **A/B 测试两种视频形态**：
   - v1：对话弹出（当前）
   - v2：波形可视化 + 逐字字幕（需要异步 TTS 时间戳）
3. **小红书/抖音内容测试**：用平台数据验证"对话体故事"的受众反馈
4. **编辑器反向接入**：把 `fast/` 验证成功的 HyperFrames 管线接回 `backend/` 的可视化工作台

---

## 9. 我的产品方法论（求职加分项）

- **约束驱动设计**：不是"有什么技术就做什么产品"，而是"产品要什么效果就选什么技术"
- **实验优先**：用最小成本验证假设（`fast/` 分支 = 2 小时实验），验证失败就换方向
- **文档即契约**：每个决策都有记录，项目可交接、可复盘
- **和 AI 协作而非指挥 AI**：把 AI 当作"懂技术的合伙人"，而不是"代码生成器"

---

**联系方式**：[你的邮箱/微信/LinkedIn]

**项目仓库**：[GitHub 链接，可开放]

**演示视频**：[可放一段 30s 的产品演示]
