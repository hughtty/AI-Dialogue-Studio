[project]
name = "dialogue-studio-backend"
version = "0.1.0"
description = "Dialogue Studio Backend API"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.110",
    "uvicorn[standard]>=0.29",
    "sqlalchemy>=2.0",
    "pydantic>=2.0",
    "python-dotenv>=1.0",
    "anthropic>=0.25",
    "httpx>=0.27",
]

[tool.uv]
dev-dependencies = []
